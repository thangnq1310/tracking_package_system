import ujson as json
import smtplib, ssl
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart

# from infrastructure.http_connector import HTTPConnectorService
#
#
# class AppleService(HTTPConnectorService):
#     def __init__(self, headers, params, url):
#         super().__init__(headers=headers, params=params, url=url)

    # def mail_to_customer(self):
    #     sender =
    #     receiver = json.dumps(self.params['customer']['email'])

